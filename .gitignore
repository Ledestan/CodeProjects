# ==================================================
# git 推送忽略文件
# ==================================================

# ==================================================
# ---------- 方法说明 -----------
# logs/ ：忽略项目中所有层级下名为 logs 的文件夹。
# /logs/ ：仅忽略项目根目录下名为 logs 的文件夹。
# *.log ：忽略项目中所有层级下以 .log 结尾的文件。
# temp ：忽略项目中所有层级下名为 temp 的文件和文件夹。
# temp/ ：忽略项目中所有层级下名为 temp 的文件夹（不匹配同名文件）。
# **/temp ：忽略任意层级下的 temp（** 为递归通配符，效果与直接写 temp/ 类似，但表达更明确）。
# ! ：取反，即放行（重新追踪）被前面规则忽略的特定文件或文件夹。
# ==================================================


# ========== IDE / 编辑器 ==========


# ========== 依赖文件 ==========
# Python 虚拟环境
.venv/
# Python 缓存文件
__pycache__/

# ========== TripScape ==========
# 训练集
Application/TripScape/data/train
# 验证集
Application/TripScape/data/valid
# 特征数组缓存
Application/TripScape/cache
# 模型  
Application/TripScape/models
